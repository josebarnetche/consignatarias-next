# Estado al 2026-05-31 — observabilidad de crons

> Snapshot local de dónde quedó el trabajo de observabilidad. No commiteado (estado de trabajo).

## ✅ Hecho y pusheado (v1.29.1, tag `v1.29.1`)

- **cron-hook cableado en los 10 crons de datos/ops**: scrape-auctions (ya estaba) +
  mag-detailed-prices, mag-lots-pipeline, mag-lots-discover, backfill-inmag, backfill-usd,
  scrape-senasa-habilitados, quota-alerts, trial-nudges, post-remate-outreach.
  Patrón: step `cron-hook start` (abre fila en `cron_runs`) + step `cron-hook finish`
  `if: always()` (cierra con ok/error según `job.status`). Aditivo — no toca la lógica del job.
- **`EXPECTED_CRONS` (`src/lib/ops.ts`)** completado: + trial-nudges (24h), monthly-close (720h),
  faena-newsletter (720h), scrape-senasa-habilitados (720h).
- **RUNBOOK** actualizado (tabla de crons + footnote).
- Typecheck OK (`npx tsc --noEmit` → 0). CHANGELOG + package.json en 1.29.1.

## ⚠️ Pendiente — 2 secrets (no urgente, degradación limpia)

El código está listo pero **inerte** hasta setear estos secrets en **GitHub + Vercel** (mismo valor).
Verificado 2026-05-31: ninguno de los dos existe (`gh secret list` solo muestra ADMIN_SECRET +
CRON_SECRET; `/api/internal/cron-hook` devuelve **HTTP 503** = not_configured).

**Consecuencia de NO setearlos:** los crons siguen corriendo y haciendo su trabajo; solo NO
aparecen en `/admin/ops` (INTERNAL_API_SECRET) y el blast El Corredor se saltea con `::warning::`
(EL_CORREDOR_BLAST_TOKEN, ~0 suscriptores hoy). Nada se rompe, no se pierde data. Es deuda de
observabilidad, no de funcionamiento. Los emails que importan (weekly/faena/monthly-close) YA salen.

### Comandos para cerrarlo (correr con `!` adelante en la sesión, o a mano)

`INTERNAL_API_SECRET`:
```bash
cd ~/consignatarias && SECRET=$(openssl rand -hex 32) && \
gh secret set INTERNAL_API_SECRET --body "$SECRET" --repo josebarnetche/consignatarias-next && \
printf '%s' "$SECRET" | vercel env add INTERNAL_API_SECRET production && \
unset SECRET && echo "OK"
```

`EL_CORREDOR_BLAST_TOKEN`:
```bash
cd ~/consignatarias && SECRET=$(openssl rand -hex 32) && \
gh secret set EL_CORREDOR_BLAST_TOKEN --body "$SECRET" --repo josebarnetche/consignatarias-next && \
printf '%s' "$SECRET" | vercel env add EL_CORREDOR_BLAST_TOKEN production && \
unset SECRET && echo "OK"
```

Notas:
- El valor se genera y consume en la misma línea; nunca se imprime ni queda en archivo.
- Cambios de env en Vercel aplican en el **próximo deploy** → redeploy si querés que tome ya.
- Validación: la ruta lee `process.env.<SECRET>`; el workflow manda `secrets.<SECRET>`. Deben coincidir.

### Verificación post-seteo (pedírselo a Claude)
1. `curl -s -o /dev/null -w "%{http_code}" -X POST .../api/internal/cron-hook` → debe dar **401** (ya no 503).
2. `gh workflow run mag-detailed-prices.yml` → confirmar fila nueva en `/admin/ops`.
