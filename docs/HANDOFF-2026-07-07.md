# Handoff — sesión 2026-07-06/07

> Estado al cierre: **v1.131.0** (`eca9ff8`). La sesión paralela va por 1.132.0 (`e1c1886`)
> y colisiona versiones constantemente — SIEMPRE `grep "^## " CHANGELOG.md | head` post-rebase
> y renumerar la propia entrada encima del tope.

## 🔴 Lo único que queda esperando algo externo

**Logging por-request de la API (v1.131.0) — armado y deployado, esperando primera pegada.**
- Tabla `api_request_log` (method·path·query·user_agent·referer·ip·ts) vía `after()` en
  `authenticate()`; vista `api_consumers_summary`; doc `docs/API-CONSUMERS.md`.
- **Tabla en 0** al cierre: el deploy salió y `changa` (el consumidor) no volvió a pegar. Se
  poblará en su próxima rueda (32 req/día, días hábiles).
- **Query pendiente de correr** cuando haya datos:
  ```sql
  SELECT * FROM api_consumers_summary WHERE owner_email = 'mapesteguia@gmail.com';
  ```
- **Qué resuelve**: si `query` incluye `?detallado=true` → Muu arma su board "Precios Hoy" con
  NUESTRA data (API). Si solo hay default/`?serie`/`?categoria` → Muu scrapea el MAG por su
  cuenta y `changa` es otra cosa (app privada de Apesteguía). El user-agent (okhttp/Dart = app
  móvil) y la IP (datacenter Rosario ≈ Merdigan) terminan de ubicarlo.

## 🟡 Investigación Muu / Apesteguía (contexto para la decisión comercial)

- La app de la captura de Jose **ES Muu Mercado Digital Ganadero** (Merdigan S.A., Rosario;
  app-id `deq.muu.app`). Confirmado por match pixel-perfect contra su captura de la Play Store.
- Fundadores Muu: Fabre / Arrighi / Rubiolo — **ningún Apesteguía**.
- Nuestra key `changa` = `mapesteguia@gmail.com` (Martín Apesteguía, 1er cliente API), pega
  **solo `/api/precios`**, 1.236 req 200, desde 14-may, 32/día hábiles. Falta la variante.
- **OJO**: el match de precios NO prueba nada (mismo MAG por cualquier camino). Jose tuvo razón
  en frenar la conclusión apurada. Nuestra API SÍ expone `?detallado=true` (16 subcats con
  min/max/head_count), así que técnicamente podría alimentar el board — el logging lo dirá.
- **Follow-ups comerciales** (cuando se sepa la variante): (1) si usa nuestra data → convertir a
  plan Growth pago (hoy consume gratis) + pedir atribución "fuente: consignatarias.com"; (2)
  mail a Martín preguntando qué alimenta `changa` — es conversación natural con un cliente.

## 🟢 Entregado esta sesión (cerrado)

- **Universo de marca — 60 oportunidades** (v1.116.0): EmptyState ×23, loading/error globales,
  21 heros/fondos, 31 emails con marca, 14 componentes, assets deployados a `public/marca/`.
  Auditoría + mapa en `docs/marca-oportunidades.md`.
- **Favicon**: C **blanca** con el cuadrado (dato) en **cielo**, transparente (`isotipo--primario.svg`).
- **Reporte visual antes/después**: artifact `claude.ai/code/artifact/27cc1275-b060-4fa0-9a01-4e941ae2b1b6`.
- **Deck inversores 10 páginas**: artifact `claude.ai/code/artifact/1fb75316-12ed-4dcf-af68-bebec78319de`.
  Pendiente OPCIONAL si lo retoma: el *ask* (monto/valuación — no lo puse sin confirmación) +
  screenshots reales del producto en vez de renders 3D.
- **Emails**: 25 templates al manual + BIMI. **Acción pendiente de Jose (DNS, manual)**: DMARC
  `p=none`→`quarantine` + registro `default._bimi` (SVG ya en `public/bimi-logo.svg`). Gmail
  además exige VMC/CMC pago; Yahoo/Fastmail gratis; atajo Gravatar. Guía en `docs/email-brand-avatar.md`.
- **El Oráculo (manifiesto)**: (1) V.3 corregida — "vacío regulatorio"→"vacío **prudencial**"
  (las consignatarias SÍ están reguladas: SENASA/DT-e/RENSPA/SIGSA/ARCA; falta solo el capítulo
  prudencial-financiero tipo CNV 731/18); (2) castellano de acá — "oracle"→pizarra/precio de
  referencia, ALyC explicado ("casi un banco, sin prestar"), fuera price-discovery/benchmark/buckets.
- **Landing El Corredor**: ahora manifest-driven (decía Edición 05/26 con junio publicado).

## ⚙️ Gotchas vigentes

- **Sesión paralela hiperactiva**: colisiones de versión diarias (hoy hasta duplicaron 1.117.0).
- **DRIFT falso**: `novillo_usd_days/series`, `visitor_stats`, `visitors`, `upsert_visitor`,
  `mag_monthly_consignatario_stats` — flaggeados por check-db-refs pero SÍ están en prod (la
  paralela los documentó en su allowlist 1.130). No re-crear; cierran al regenerar types.
- **Credenciales en auto-mode**: `vercel env pull` de producción + minteo de keys `cnsg_live_*`
  lo bloquea el clasificador (bien). El self-test del logging quedó sin correr por eso — se
  auto-verifica con tráfico real.
