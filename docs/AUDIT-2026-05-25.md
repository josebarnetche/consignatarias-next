# Audit 360° — estado actual (2026-05-25)

> Snapshot para retomar. Audit hecho con un swarm de 4 agentes (técnico/SEO, producto,
> customer journeys, marketing). **IMPORTANTE: verificar cada hallazgo contra el código
> antes de actuar** — el agente de marketing resultó impresionista y metió errores
> (ver "Errores detectados"). Los de técnico/producto/journeys leyeron código/corrieron
> scripts y son sólidos, pero igual confirmar.

## Contexto: de dónde venimos
Iniciativa long-tail/SEO cerrada en 4 PRs mergeados (#15–#18). **Site-wide thin pages: CRITICAL ~1385 → 288, OK → 325.**
- frig-detail 806→69 críticas (desgateo SENASA + summary + módulo provincia + existencias).
- remate-detail 403→40 (módulos related + resumen data-derived + precio categoría + raza).
- consig-detail (overview server-side) + noindex de las thin (0-1 remate).
- Brand wall en el home (40 logos), HK Agro al scraper + logo, fix Colombo y Colombo (provincia).
- Existencias SENASA por provincia (dato real 2019). Política: frigoríficos TODOS indexados; consig 0-1 remate noindex.

## ⚠️ Errores detectados en el propio audit (no confiar ciegamente)
- **FALSO:** "`/precios` y `/mercado/inmag` no tienen captura de email / zero conversion."
  - **Realidad (verificada en código):** `/mercado/inmag` importa `CierreMensualSubscribe` (captura email → POST /api/newsletter) + `ProUpgradePrompt` (upsell PRO) + `ElCorredorCTA`. `/precios` es una página-hub (165 líneas) que rutea a hacienda-en-pie / mercado/inmag / spread / inmag-dolares; deriva a inmag que sí captura.
  - Lección: el agente de marketing juzgó por screenshot/curl sin leer componentes. Re-verificar todo lo "impresionista".

## Hallazgos — clasificados por confiabilidad

### ✅ Sólidos (scripts corridos / código leído con paths)

**Técnico/SEO (corrió los 4 audit scripts contra localhost:3000):**
- 288 CRITICAL · 1.261 WARNING · 325 OK · 52 near-dup clusters (content-audit).
- Link graph: 290 huérfanos (casi todos remate-detail) · 1.035 weak (<3 inbound) · 149 broken internal targets · 220 anchor monocultures · 2 outbound-spam.
- **P0 contradicción noindex↔sitemap:** consignatarias thin con `robots:{index:false}` (PR #18) SIGUEN emitidas en `src/app/sitemap.ts:361` (`getAllCanonicalSlugs()` sin filtro). Verificado: `/consignatarias/aj-mendizabal` da noindex pero está en sitemap.xml. → filtrar los thin del sitemap.
- **P0 `/frigorificos/verificar`** linkeado desde ~1.091 páginas, NO bloqueado en robots (el `Disallow: /verificar` no cubre ese path) → fuga de crawl-budget. Agregar disallow o noindex.
- Frig sin noindex = **by-design** (Jose pidió "indexar todo"); el agente lo marcó pero NO es bug.
- Páginas comerciales PageRank-starved: `/precios` 1 inbound, `/enterprise` 2, `/api-docs` 4, vs `/planes` 1.263. Linking interno alimenta hojas de directorio, no las de plata.
- 52 near-dup: remates recurrentes (misma consignataria+tipo+provincia, distintas fechas) se canibalizan (~85% shingles iguales).
- Data freshness: market-prices.json quedó ~3.8 días viejo (cron de finde no commiteó). NO es código.
- api-health no corrió: falta `SUPABASE_SERVICE_ROLE_KEY` en `.env.local` local.
- **Schema/structured data = excelente** (Event/LocalBusiness/Breadcrumb/FAQ/Dataset, 37 @types). Perf buena (103kB shared). Metadata sólida. NO tocar.

**Producto (leyó código + conteos DB):**
- **P0 `/api/lots` (diferencial Enterprise) = 0 filas en prod.** `mag-lots-pipeline.yml` nunca pobló. Se vende acceso a endpoint hueco. La "infraestructura" hoy = wrapper INMAG (coincide con autocrítica Sesión 8).
- **P1 funnel PRO Consignataria roto:** `planes/SubscribeButton.tsx` manda `entitySlug || 'unknown'` → el webhook no puede mapear → `featured` nunca se activa tras pagar. Y `/api/claims` NO envía el magic-link (usa `generateLink()` que no emailea; TODO sin hacer) → claim no es self-serve.
- `mag_prices_detailed` (16 subcats) solo 6 días de historia → los índices del roadmap (Liquidation, etc.) no tienen base.
- El Corredor / El Oráculo = 1 PDF suelto, sin cadencia → mata la promesa "reportes frescos" de PRO Usuario.
- **Mi Ganado** = mejor hook de retención (DB-backed, RLS), infrautilizado (1 usuario). Promocionarlo.
- Subsistema de **alertas muerto** (`/api/alertas` usa auth incompatible `sk_live_` + cron en disabled/). Revivir sobre `cnsg_live_` o borrar.
- Drift: enterprise/page dice lots "próximamente" mientras /api/lots está construido (y vacío). ROADMAP v1.14.5 vs CLAUDE v1.18.0.
- Pre-revenue: 1 PRO Usuario, 2 Enterprise (1 founder comp), 0 entity subs, 1 claim real.

**Customer journeys (curl/Chrome en vivo):**
- **P0 `/consignatarias/[slug]/remitentes` 100% roto:** solo `monasterio-tattersall-s-a` tiene data; el page hace 308 a `/consignatarias/monasterio-tattersall/remitentes` que da 404 (canonical nunca se agregó a generateStaticParams). Resto → 404 directo. Y no hay link entrante. Feature muerto.
- **P1 listado de remates NO linkea a `/remates/[slug]`:** `RematesClient.tsx getAuctionHref` linkea al source externo / perfil consignataria. → las páginas de detalle (que enriquecimos) quedan huérfanas. **Arreglar esto rescata ~290 huérfanos + activa el SEO de remate-detail.**
- P1 contacto del comprador indirecto: WhatsApp en card es "compartir" (no línea directa); contacto real gateado tras PRO en el perfil.
- P1 `/upgrade` = solo login, no checkout (el real está en `/planes`).
- P1 conteos inconsistentes UI: 104 / 82 / 74 consignatarias; 12/13/14/16 provincias.
- P2 mobile 390px: overflow horizontal en hero + topbar (texto cortado).
- P2 newsletter/El-Corredor POST dan 500 LOCAL (falta service key; en prod anda).
- Verificado OK: nav/footer todo 200, directory→detail resuelve, brand-wall logos 200, /mercado real, error-states 404 correctos.

### 🟡 Impresionista (re-verificar antes de usar)
**Marketing** — ya falló en lo de captura. Lo rescatable (a confirmar): mismatch posición↔tráfico (hero vende "consignatarios", tráfico real = precio/INMAG + frigoríficos/SENASA), `/planes` mezcla 3 compradores en una tabla, falta one-liner de metodología/independencia en páginas comerciales, propuesta de reposicionar sobre "el precio/INMAG" (Ries & Trout). NADA de esto verificado a fondo.

## Próximos pasos (orden de ROI, verificar c/u primero)
1. **Listado remates → `/remates/[slug]`** (`RematesClient.tsx getAuctionHref`) — rescata huérfanos + SEO ya hecho. *Quick, código.*
2. **noindex↔sitemap** — filtrar thin/noindexados de `sitemap.ts`. *Trivial.* + bloquear `/frigorificos/verificar` en robots.
3. **`/remitentes` 308→404** — arreglar canonical en generateStaticParams o noindex+remover.
4. **Poblar `mag_consignataria_sales_lots`** (mag-lots-pipeline.yml) — convierte Enterprise en producto real. *Data, no código.*
5. **Funnel PRO Consignataria** — sacar `'unknown'` + enviar magic-link (signInWithOtp/Resend).
6. (Estratégico, decisión Jose) Reposicionamiento landing sobre precio/INMAG — verificar el estado real del hero/CTAs primero.

## Estado operativo
- Todo mergeado en `main` (último: PR #18 noindex thin consig). Working tree limpio.
- Server de audit frenado, puerto 3000 libre, sin procesos colgados.
- Repo: `~/consignatarias`. Vercel project `consignatarias-next`. Build SSG ~2.506 páginas, exit 0.
- Audit scripts: `scripts/audit-{content-quality,link-graph,data-integrity,api-health}.mjs` → `scripts/.cache/`.
