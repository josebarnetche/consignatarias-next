# Handoff — 2026-05-29

> Para retomar mañana en frío. Estado al cierre de la sesión del 29-may.

## TL;DR
Se shippeó (commit `c6a6a18`, push a `main`, deploy prod Ready) dos cosas:
1. **Trial-nudges** — emails automáticos 7 y 3 días antes de que venza una prueba free-credits. Sin corte de acceso.
2. **Fix del contador "En Vivo" de la home** — pasó de 0 a ~137 "posibles transmisiones" (estimado por canal).

Después se corrió una **eval completa del sitio** (5 audits). Veredicto: sólido pero NO perfecto — hay 1 P0 de duplicados, cola larga de páginas thin de SEO, y `/api/lots` a revisar. Nada urgente-roto.

---

## 1) Lo que se deployó hoy (c6a6a18)

### Trial-nudges (pedido original)
- **Cron:** `src/app/api/cron/trial-nudges/route.ts` — escanea `pending_api_invites.free_credits=true` redimidos, calcula `api_tier_activated_at + 30d`, dispara nudge a los **7d** (faltan ≤7 y >3) y a los **3d** (≤3), una sola vez cada uno. **NO pausa acceso.**
- **Email:** `sendTrialNudge()` en `src/lib/email.ts` — tono sutil ("llevás X requests / ¿cómo las venís usando?"), promociona TODAS las features + plan **Growth**, `from: noreply@consignatarias.com`, **`replyTo: agro@memola.com.ar`**.
- **Workflow:** `.github/workflows/trial-nudges.yml` — diario 10:17 ART (GitHub Actions, como los otros 14).
- **Migración:** `supabase/migrations/20260529_trial_nudges.sql` — columnas `trial_nudge_7d_at` / `trial_nudge_3d_at` en `user_subscriptions`. **YA APLICADA en prod** vía MCP.

**Cliente en prueba ahora:** `mapesteguia@gmail.com` = **Martín Apesteguia** (key "changa", tier starter, "primer user dev, valorización rodeos app"). Activó 13-may → **prueba vence 12-jun 17:06 ART**.
- Nudge 7d → sale **~5-jun**. Nudge 3d → sale **~9-jun**. El 12-jun NO se corta nada (decisión explícita de Jose).
- Consumo: ~30 req/día, ~35% del cupo starter (1.000/período de 28d). Candidato a upgrade Growth (50.000).

**Para testear antes del 5-jun:** `gh workflow run trial-nudges.yml` (dispara manual; con días_restantes=14 hoy no manda nada, es no-op seguro). O bajar temporalmente `TRIAL_DAYS` para probar el envío real.

### Fix contador lives (home)
- `src/app/page.tsx` ahora usa `resolveYoutubeUrl()` (de `src/lib/youtube-live.ts`) — mismo criterio que `/remates/en-vivo`: confirmadas (video directo) + estimadas (canal habitual de la consignataria). Etiquetado honesto: "Posibles en vivo", "estimado · por canal", "N posibles transmisiones".
- **Por qué daba 0:** el matcher (`scripts/match-youtube-videos.ts`) solo pega `youtubeUrl` a remates de HOY el mismo día → los próximos nunca tenían stream. La home contaba solo `youtubeUrl` directo. Verificado en prod: 137.

---

## 2) Eval del sitio — backlog priorizado (NO empezado)

Reportes completos en `scripts/.cache/`: `data-integrity-report.md`, `api-health-report.md`, `link-graph-report.md`, `content-audit-report.md`, `404-candidates.json`.

### P0 — duplicados (1)
- **5 remates duplicados por slug-variant** (mismo canonical+tipo+provincia+fecha): `colombo-y-magliano`, `monasterio-tattersall`, `mondino`. Ver detalle en `data-integrity-report.md`. Causa probable: el scraper/canonical map deja entrar 2 slugs del mismo. Fix en `scripts/scrape-auctions.mjs` o dedup post-scrape por (canonical,type,province,date).

### SEO / contenido — el de mayor impacto
- **45/150 páginas thin CRÍTICAS** (~30%) + 36 warning. Tres clusters:
  - `remates/[provincia]/[tipo]` — 18 críticas (combos vacíos, ej. `/remates/corrientes/reproductores` 30 palabras).
  - `consignatarias/[slug]` — 15 críticas (perfiles zombie: `h-nieva` 22 palabras, `arzoz`, `ganados-remates`).
  - `consignatarias/[provincia]` — 9 críticas (`santiago-del-estero` = 3 palabras únicas).
  - **Acción:** noindex o consolidar (ya hay precedente: PR #18 noindex thin consig). Ampliar el mismo patrón a province×type vacíos.
- **2 orphans** (0 inbound): `/el-oraculo`, `/remates/fin-de-semana` — linkearlos o sacarlos del sitemap.
- **~1390 targets linkeados fuera del sitemap.** Sospechoso real: **`/consignatarias/tucuman` linkeado por 12 páginas** (tucumán es provincia, no consignataria → link mal generado, buscar dónde se arma).
- 39 weak pages (<3 inbound), 32 anchor-text monocultures.

### Higiene de datos (P2)
- **7 perfiles "uncurated"** sintéticos no indexables, incluye **`ghc-logo`** (slug basura colado — investigar de dónde sale). Otros: `atreuco`, `aristegui`, `ganados-remates-s-a`, `bermejo-y-cia3`, `cyg-n-hacienda2`, `sociedad-agricola-ganadera-ltda2`.
- 10 consignatarias zombie (sin remates 60d).
- Sitemap emite `/remates/tucuman` sin remates hoy → página vacía indexable.
- Tabla `remates` de Supabase desactualizada (último `updated_at` 09-mar) — el JSON `src/lib/data/remates.json` es la fuente real. Mirror legacy a limpiar/documentar.

### API
- **`/api/lots`: 1 llamada, 100% error** — revisar antes de venderlo en Growth.
- Ruta zombie `/api/index/memola` (0 tráfico 30d) — código muerto, borrar.

### Comercial
- 1 PRO de pago (changa/Martín, starter). Outreach: 58 enviados, **0 respuestas trackeadas** — ¿el tracking de `outreach_log.response_received_at` no se carga, o nadie responde? A confirmar.

---

## 3) Orden sugerido para mañana
1. **P0:** dedup de los 5 remates (alto impacto, bajo esfuerzo).
2. **SEO:** noindex/consolidar las 45 thin pages + slug `ghc-logo`.
3. **Bug:** link `/consignatarias/tucuman` (×12).
4. **API:** debug `/api/lots` 100% error.
5. (Opcional) limpiar ruta zombie `/api/index/memola`, orphans, tabla `remates` legacy.

## Cómo re-correr los audits
```bash
cd ~/consignatarias
set -a; . ./.env.local; . ./.env.analytics.local; \
  [ -z "$SUPABASE_URL" ] && export SUPABASE_URL="$NEXT_PUBLIC_SUPABASE_URL"; set +a
node scripts/audit-data-integrity.mjs
node scripts/audit-api-health.mjs
BASE=https://www.consignatarias.com.ar CONCURRENCY=8 LIMIT=200 node scripts/audit-link-graph.mjs
BASE=https://www.consignatarias.com.ar CONCURRENCY=8 LIMIT=150 node scripts/audit-content-quality.mjs
```

## Refs rápidas
- Vercel project: `consignatarias-next` (Hobby). Deploy = push a `main` (auto). OJO: el scraper pushea data a `main` solo → si rechaza push, `git rebase origin/main` (sus commits solo tocan JSONs).
- Supabase project: `nyqkgorazkwcufkzxmhd` (MCP wired).
- Versión: v1.18.0. Sitemap: 1924 URLs.
